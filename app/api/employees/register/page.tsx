import { EmployeeAccountRegister } from "@/components/api/employees/register/EmployeeAccountRegister";
/**
 * アカウント登録画面コンポーネントを実装し動作確認する
 * URL: /admin/account/form のルーティングページ
 */
export default function Page() {
     <main className="container mx-auto py-8"></main>
    return <EmployeeAccountRegister />;
}